<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>nd_Lottery_Always_Review__c</fullName>
    <formula>IF(
    NOT(ISBLANK(Constituent__c)),
    Constituent__r.nd_Lottery_Always_Review__c,
    IF(
        NOT(ISBLANK(Account__c)),
        Account__r.nd_Lottery_Always_Review__c,
        FALSE 
    )
)</formula>
    <label>Lottery Always Review</label>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Checkbox</type>
</CustomField>
